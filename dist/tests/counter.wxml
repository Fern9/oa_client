
  <view class="counter {{style}}">
    <button bindtap="plus" size="mini">  +  </button>
    <button bindtap="minus" size="mini">  -  </button>
    <text class="count {{$undefined$num > 55 ? 'red' : ''}} {{$undefined$num < 45 ? 'green' : ''}}" > {{num}} </text>
  </view>